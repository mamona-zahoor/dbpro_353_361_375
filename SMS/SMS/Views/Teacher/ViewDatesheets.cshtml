@model IEnumerable<SMS.Models.DatesheetVM>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClassName)
        </th>
       
    </tr>
    <tr></tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClassName)
        </td>
      
        <td>
          
           
            @Html.ActionLink("View Datesheet", "ViewDateSheet", new {  id=item.ClassId }) |
          
          
        </td>
    </tr>
}

</table>
